/**
 * extraReducers :
 * 
 * We can add slice of one reducer to another reducer.
 *  - While ordering cake from cake reducer we can also change the state of ice-cream reducer 
 * 
 * createAsyncThunk :
 * 
 * - return promises.
 * 
 */